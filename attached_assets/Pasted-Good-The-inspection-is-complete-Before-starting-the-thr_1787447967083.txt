Good. The inspection is complete. Before starting the three implementation phases, perform the following catalogue/SDF verification step.

STEP 0 — CHECK THE ATTACHED SDF CATALOGUE FILES FIRST

I have already attached the SDF catalogue files to this project/workspace.

Before changing the application or starting Phase 1:

1. Inspect the project files and locate all SDF files that are already attached/uploaded in the project.
2. Do NOT ask me to upload them again if they are already present.
3. Do NOT create a new fake SDF file.
4. Do NOT replace the attached files with mock medicine data.
5. Determine exactly which SDF file(s) are available and which one is intended for the Ayush Foundation catalogue.
6. Inspect the existing SDF importer/parser and confirm that it can read the attached file format correctly.
7. If the existing project already has an upload/import UI, connect or use the already-attached SDF file through that existing workflow rather than creating a duplicate importer.
8. If the file is already imported into PostgreSQL, verify the imported records instead of importing duplicates.
9. If it has not yet been imported, use the existing controlled SDF import mechanism to import the attached file into the existing Ayush Foundation catalogue tables.
10. Preserve the existing idempotent import behavior and source identifiers.

CATALOGUE VERIFICATION

After checking/importing the SDF file, verify the actual catalogue data from PostgreSQL/API.

I want the real medicines from the attached SDF file to appear in the Ayush Foundation medicine catalogue.

Verify at minimum:

- Total medicine/product records imported
- Medicine/product names
- Company/manufacturer data where available
- Categories where available
- Strength/form data where available
- Existing stock/price fields where actually present
- Search functionality
- Category filtering
- Autocomplete
- Pagination/loading if already implemented

Do NOT invent missing values.

If a field is not present in the SDF data, leave it unavailable rather than generating fake information.

IMPORTANT: SHOW THE MEDICINES

After the SDF verification/import, make sure the public Ayush Foundation catalogue page actually displays the imported real medicine records.

The catalogue should be populated from the PostgreSQL/API data, not hardcoded frontend arrays.

Verify this using the running application/API where possible.

If the database is not configured yet, do not fake the result. Report exactly what configuration is missing and what was successfully verified from the attached files.

Only after this SDF/catalogue verification is complete should you begin PHASE 1 below.

We will now continue implementation in THREE SEPARATE PHASES. Do not attempt all remaining functionality in one pass.

Do NOT stop at the inspection report and do NOT ask me what to implement next.

PHASE 1 — FOUNDATION SETUP + CATALOGUE + CUSTOMER AUTH

Only work on Phase 1 in this pass. Do NOT start checkout, prescriptions, orders, or payments yet.

PHASE 1A — FOUNDATION SEPARATION

Before implementing customer/order functionality, fix the Ayush Foundation separation issue you discovered.

Search the entire active project for old Ayush Medico references, including:

- UI text
- page titles
- Firebase configuration references
- Cloudinary folder/path references
- environment variable names
- API configuration
- comments/config files where they affect runtime behavior

Replace old project-specific references only where they are actually part of Ayush Foundation's configuration or branding.

IMPORTANT:

Do NOT blindly change unknown Firebase/Cloudinary credentials.

If the current credentials point to Ayush Medico and there is no confirmed Ayush Foundation credential available, do NOT invent credentials.

Instead:

1. Identify every required Foundation-specific credential.
2. Keep the application architecture ready for the Foundation credentials.
3. Clearly list the exact environment variables/secrets that must be supplied.
4. Do not pretend the Foundation integration is configured until it actually is.

Do the same for Cloudinary.

PHASE 1B — DATABASE AND CATALOGUE

Inspect the existing PostgreSQL schema and catalogue implementation again.

The existing real catalogue is valuable and must be preserved.

Do NOT:

- create fake medicines
- create fake prices
- create fake stock
- replace the catalogue with mock JSON
- duplicate catalogue tables unnecessarily

Connect the existing public catalogue properly to the existing database/API.

Verify that medicine search, categories, and product data are coming from the real catalogue.

If DATABASE_URL is missing, implement the code correctly but report that database configuration is required before live verification.

PHASE 1C — CUSTOMER AUTHENTICATION

Implement customer authentication using the existing planned Ayush Foundation architecture.

Customers need:

- Sign Up
- Sign In
- Sign Out
- Session persistence
- Customer profile
- Protected customer pages

Keep admin authentication completely separate from customer authorization.

Do NOT weaken the existing admin protection.

Server-side APIs must verify the authenticated customer before accessing customer-specific resources.

PHASE 2 — MEDICINE DETAILS + CART + CHECKOUT + PRESCRIPTIONS

Do not start Phase 3 until Phase 2 is explicitly requested.

PHASE 2A — MEDICINE DETAILS

Implement a real medicine/product details page using the existing catalogue.

The page should use the actual product data and support:

- Medicine name
- Company/brand
- Relevant medicine information already available in the catalogue
- Current available price where provided by the database
- Stock/availability where provided
- Prescription requirement
- Add to Cart

Do not invent missing medical/product information.

PHASE 2B — CART

Implement a proper customer cart.

The cart must support:

- Add product
- Remove product
- Increase/decrease quantity
- Quantity validation
- Cart total
- Prescription-required item detection
- Empty cart state
- Persistence for the customer's session

Do not trust frontend prices.

The server must re-check product information before creating an order.

PHASE 2C — CHECKOUT

Implement the customer checkout flow.

Customer should provide:

- Name/profile information
- Mobile number
- Delivery address
- Landmark if required
- Delivery notes if required

Before creating an order, the server must re-check:

- Product exists
- Product is available
- Current price
- Requested quantity
- Stock
- Prescription requirement

Do not allow the frontend to decide the final order price.

PHASE 2D — PRESCRIPTION WORKFLOW

Implement prescription handling for medicines that actually require a prescription.

Rules:

- Prescription-required products require a prescription.
- Non-prescription products should not require one.
- Prescription uploads must be securely handled.
- Customer must be able to attach the prescription during checkout.
- Admin must be able to review the prescription.
- Admin can approve or reject it.
- The order workflow must respect the prescription decision.

Do not create fake prescription records or fake approvals.

PHASE 3 — ORDERS + STOCK + ADMIN + PAYMENTS

Do not start Phase 3 unless Phase 2 has been completed and verified.

PHASE 3A — ORDERS

Connect the existing order database structures to the real customer checkout.

Implement:

Customer:
Browse
→ Cart
→ Checkout
→ Place Order
→ Order Confirmation
→ My Orders
→ Order Details
→ Status Tracking

Admin:
Orders
→ View Order
→ Review Items
→ Review Prescription if required
→ Approve/Reject
→ Process Order
→ Update Status

Use server-side authorization for all order operations.

Customer A must never be able to view Customer B's order.

PHASE 3B — STOCK

Connect stock validation to order creation.

Do not blindly decrement stock from the frontend.

Use a safe server-side transaction/reservation approach appropriate for the existing PostgreSQL/Drizzle architecture.

If the current schema needs a small addition to support this safely, make the minimum necessary schema change.

Do not destroy existing catalogue data.

PHASE 3C — PAYMENT

Payment integration is currently NOT configured.

Therefore:

Do NOT claim payment is working.

Do NOT create fake successful payments.

Do NOT automatically mark orders as paid.

Instead, implement the payment architecture so that the real provider can be connected later.

If a payment provider is already partially present in the code, inspect it and preserve it.

At the end, tell me exactly which payment provider credentials/configuration are still required.

PHASE 3D — ADMIN

Extend the existing Admin Orders section instead of replacing it.

Admin should eventually be able to:

- View incoming orders
- View customer/order details
- View order items
- Review prescriptions
- Approve/reject prescriptions
- Process orders
- Update order status
- View payment status
- View relevant delivery information

All admin actions must be server-authorized.

PHASE 3E — FINAL TESTING

After implementation:

1. Run TypeScript checks.
2. Run production build.
3. Run available tests.
4. Verify frontend routes.
5. Verify API routes.
6. Verify authentication guards.
7. Verify customer authorization.
8. Verify admin authorization.
9. Verify cart logic.
10. Verify checkout validation.
11. Verify prescription-required logic.
12. Verify order creation.
13. Verify stock validation.
14. Verify that existing catalogue functionality still works.

Fix errors you encounter.

PHASE CONTROL RULE

When I send this prompt, implement PHASE 1 ONLY.

At the end of Phase 1, stop implementation and report exactly what was completed, what was tested, and what remains for Phase 2.

Do NOT automatically continue into Phase 2 or Phase 3.

When I later say "Start Phase 2", implement only Phase 2.

When I later say "Start Phase 3", implement only Phase 3.

This phased approach is intentional because the project is large and must be implemented and verified incrementally.

CRITICAL RULE

Do NOT stop after writing code.

Actually inspect the result and test it.

If a required external credential is missing, do everything that can safely be implemented without it, then report the missing credential clearly.

Do NOT use placeholder success states that could be mistaken for real production functionality.

FINAL REPORT

When this implementation pass is complete, report:

- What you actually implemented
- Files changed
- Database/schema changes
- API routes added/changed
- Customer authentication status
- Catalogue status
- Cart status
- Checkout status
- Prescription status
- Order status
- Stock status
- Payment status
- Tests/build results
- Exact remaining configuration required

Do not continue into another phase after the current phase is complete.

This is an implementation task for the CURRENT PHASE ONLY. Continue coding from the existing Ayush Foundation project without rebuilding it.