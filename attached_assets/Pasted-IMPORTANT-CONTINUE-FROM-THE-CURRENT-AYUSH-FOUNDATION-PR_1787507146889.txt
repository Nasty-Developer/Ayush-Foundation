IMPORTANT — CONTINUE FROM THE CURRENT AYUSH FOUNDATION PROJECT.

I HAVE NOW CREATED A FRESH, SEPARATE AYUSH FOUNDATION FIREBASE PROJECT AND I HAVE ALREADY ADDED ALL REQUIRED FIREBASE CLIENT AND FIREBASE ADMIN SECRETS TO THE REPLIT PROJECT ENVIRONMENT.

DO NOT ASK ME FOR THE FIREBASE VALUES AGAIN.

DO NOT STOP JUST BECAUSE THE VALUES ARE SECRET.
DO NOT PRINT, EXPOSE, LOG, OR REVEAL ANY SECRET VALUES.

Your job now is to VERIFY AND FIX THE CONFIGURATION YOURSELF.

==================================================
1. FIX FIREBASE CONFIGURATION NOW
==================================================

Use ONLY the Firebase configuration currently stored in the Replit environment.

The correct Ayush Foundation Firebase project is:

Project ID:
ayush-foundation-f6d89

Auth domain:
ayush-foundation-f6d89.firebaseapp.com

Verify the runtime is actually reading the current Replit secrets instead of any stale/legacy values.

Check for:

VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
VITE_FIREBASE_MEASUREMENT_ID

And Firebase Admin:

FIREBASE_PROJECT_ID
FIREBASE_CLIENT_EMAIL
FIREBASE_PRIVATE_KEY

NEVER expose their values.

If source code, .env files, .firebaserc, cached configuration, or build configuration still references:

ayush-medico-6cf77

REMOVE/FIX ONLY THE STALE REFERENCE.

Do not invent any Firebase value.

Do not replace the fresh Foundation secrets with anything else.

After fixing the configuration, restart/rebuild the relevant frontend and API processes so they actually pick up the new environment.

Then verify that the runtime resolves to:

ayush-foundation-f6d89

Do not merely check that the secret names exist.

==================================================
2. FIX FIREBASE AUTHENTICATION
==================================================

Now verify the actual customer authentication implementation.

Test:

Sign Up
Sign In
Sign Out
Session persistence

Use the Ayush Foundation Firebase project only.

Do NOT modify or weaken admin authentication.

If Google Sign-In is already enabled in the Foundation Firebase project and the code supports it, verify it.

If Google Sign-In is not enabled, do NOT fake it.

Instead report exactly what Firebase configuration is still required.

==================================================
3. FIX THE REAL MEDICINE CUSTOMER FLOW
==================================================

Now continue with the existing Phase 2 work.

DO NOT rebuild the application.

DO NOT re-import the catalogue unless inspection proves it is actually missing/corrupt.

DO NOT create fake medicines.

The PostgreSQL catalogue already contains approximately:

51,231 products
2,322 companies
16 categories
1,099 drug groups
10,871 stock records

Make sure the public customer catalogue actually works.

Test:

Homepage
→ Medicines
→ Search
→ Category
→ Medicine Details
→ Price
→ Add to Cart
→ Cart
→ Sign In
→ Checkout

Actually test the running application.

Do not rely only on TypeScript/build success.

==================================================
4. PRICE + ADD TO CART
==================================================

Fix any remaining issue where:

- Medicine price is missing
- Add to Cart does not work
- Cart receives incorrect product ID
- Cart receives incorrect price
- Quantity does not work
- Cart total is incorrect
- Cart disappears unexpectedly after refresh

Use authoritative database/API data.

NEVER invent prices.

If a real price cannot safely be linked to a product, show an honest unavailable-price state.

Do not randomly map the 10,871 stock rows.

==================================================
5. MEDICINE IMAGES
==================================================

Every medicine should have a professional visual.

If a real product image exists, use it.

If not, use the existing neutral generic medicine/tablet fallback image.

Make sure there are no broken image icons or empty image containers.

Use the fallback consistently on:

- Medicine cards
- Medicine details
- Search results
- Homepage medicine sections
- Categories

==================================================
6. EXACT THREE CUSTOMER CATEGORIES
==================================================

Customer-facing categories must be:

💊 Tablet Medicines
🐾 Vet Medicines
General Medicines

Vet Medicines MUST remain empty.

Do NOT populate Vet Medicines with human medicines.

Show a polished:

"Veterinary medicines coming soon."

state.

Use real catalogue data for Tablet and General medicines where classification is actually trustworthy.

Do not fabricate classifications.

==================================================
7. CHECKOUT
==================================================

Once Add to Cart works, verify Checkout.

Checkout must collect:

Full Name — required
Mobile Number — required
Delivery Address — required
Landmark — optional
Email — optional
Delivery Notes — optional

Validate everything properly.

The server must re-check:

Product
Price
Quantity
Availability
Customer identity
Prescription requirement

Do not trust frontend prices or customer IDs.

==================================================
8. PRESCRIPTION
==================================================

If a real catalogue medicine requires a prescription:

Require prescription upload.

If it does not:

Do not unnecessarily require one.

Validate uploads server-side.

Do not expose prescription files publicly.

Do not fake prescription approval.

==================================================
9. ORDER CREATION
==================================================

Use the existing Ayush Foundation order architecture if already implemented.

A customer should be able to reach the legitimate order-submission stage.

Do NOT fake payment success.

Do NOT fake delivery success.

If payment is not configured yet, leave the order in the legitimate pending/unpaid state.

Do NOT start the full Phase 3 payment/delivery/admin workflow.

==================================================
10. VERCEL — DO THIS AFTER LOCAL FLOW WORKS
==================================================

This is critical.

The final goal is the LIVE VERCEL Ayush Foundation website.

Inspect:

- GitHub repository
- main branch
- Vercel project
- Root Directory
- Build settings
- Production environment variables
- API deployment
- Database connection
- Firebase configuration

The correct GitHub repository is:

https://github.com/Nasty-Developer/Ayush-Foundation

Make sure Vercel is deploying THIS repository and not Ayush Medico or an old deployment.

If the latest code is not on GitHub main, commit and push the current Ayush Foundation changes safely.

DO NOT force push.

==================================================
11. VERCEL FIREBASE CONFIGURATION
==================================================

The same fresh Ayush Foundation Firebase configuration must exist in Vercel Production environment variables.

Do not assume Replit secrets automatically exist on Vercel.

Check Production environment specifically.

Configure the required Firebase client variables there using the already supplied Foundation values.

Configure the required Firebase Admin variables where the API deployment requires them.

NEVER expose their values in logs or the final report.

After changing Vercel environment variables, trigger a fresh production deployment.

==================================================
12. ACTUAL PRODUCTION TEST
==================================================

Do NOT say Vercel works just because deployment succeeded.

Open the actual production URL and test:

Homepage
→ Medicines
→ Tablet Medicines
→ Real Medicine
→ Price
→ Add to Cart
→ Cart
→ Sign In
→ Checkout

Also test:

Vet Medicines → empty state

General Medicines → real medicines

Authentication must use:

ayush-foundation-f6d89

NOT:

ayush-medico-6cf77

==================================================
13. IF VERCEL ACCESS IS NOT AVAILABLE
==================================================

Do not repeatedly stop and ask me for the same Firebase values.

The Firebase values are already supplied.

If Vercel access itself is genuinely unavailable, continue everything that can be completed locally and with GitHub.

Then report ONLY the actual external blocker, for example:

"Vercel project access is unavailable."

Do not claim deployment is complete.

==================================================
14. IMPORTANT — DO NOT LOOP
==================================================

We have already spent multiple sessions discovering the same Firebase issue.

DO NOT repeat the same discovery pass.

DO NOT tell me again:

"Firebase values are missing"

unless you have actually verified that the environment variables are absent.

They have already been supplied.

If the runtime is still resolving the legacy project, FIX THE RUNTIME CONFIGURATION YOURSELF.

Do not simply report the problem.

==================================================
15. PHASE BOUNDARY
==================================================

This is still Phase 2 customer-flow work.

DO NOT start:

- Full payment integration
- Delivery management
- Advanced stock reservation
- Full admin order processing
- Full order tracking

First make this genuinely work:

SEE MEDICINE
→ SEE PRICE
→ SEE IMAGE
→ ADD TO CART
→ CART
→ SIGN IN
→ CHECKOUT

Then verify the same flow on Vercel.

==================================================
FINAL RULE
==================================================

DO NOT STOP AFTER BUILD SUCCESS.

DO NOT STOP AFTER TYPECHECK.

DO NOT STOP AFTER SECRET DISCOVERY.

ACTUALLY FIX THE CONFIGURATION AND TEST THE RUNNING APPLICATION.

If something fails, diagnose and fix it.

Only stop when you reach a genuine external blocker that cannot be solved from the current project/environment.

At the end, give me a concise report containing:

- Firebase runtime project verified
- Authentication status
- Catalogue status
- Price status
- Add-to-cart status
- Cart status
- Checkout status
- Prescription status
- GitHub status
- Vercel deployment status
- Production customer-flow test
- Exact remaining blocker, if any

DO NOT EXPOSE ANY SECRET VALUES.