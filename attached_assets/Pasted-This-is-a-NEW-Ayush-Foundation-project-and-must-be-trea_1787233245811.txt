This is a NEW Ayush Foundation project and must be treated independently from the old Ayush Medico inventory-sync project.

I want to completely avoid the old recurring/live inventory synchronization architecture.

NEW REQUIREMENT:

I will provide the official SDF medicine data files directly.

The goal is simply:

SDF files
→ parse the data
→ import the medicine catalog into this project's database
→ make all imported medicines searchable/displayable on the website.

There must be NO recurring inventory synchronization system.

Do NOT implement or copy the old:
- sync_sessions
- /api/sync/session
- /api/sync/start
- active-sync locks
- sync heartbeat system
- sync cancellation workflow
- 15-minute/2-hour sync leases
- inventory-sync UI
- recurring SDF synchronization

First inspect this NEW repository's existing architecture, database schema, medicine/product tables, category tables, company/brand tables, and medicine search implementation.

Do not modify anything until you understand the existing structure.

I will provide these SDF files:

- PRODUCT.SDF
- STOCK.SDF
- COMPANY.SDF
- CATEGORY.SDF
- DRUG.SDF

Build a controlled one-time import mechanism specifically for these files.

IMPORTANT DATA REQUIREMENT:

Do NOT import only the medicine names.

Preserve the relationships and useful information available in the SDF files.

The final medicine catalog should support:

1. Medicine/product name
2. Brand/company/manufacturer
3. Category
4. Drug group
5. Product details
6. Stock information where available
7. Medicine/product identifiers from the source data
8. Other useful source fields where they map cleanly to the existing schema

CATEGORY REQUIREMENT:

Every imported medicine/product must be connected to the appropriate category whenever the SDF data provides one.

BRAND/COMPANY REQUIREMENT:

Every imported medicine/product must be connected to the correct company/brand/manufacturer whenever the SDF data provides one.

The website should therefore be able to display and filter medicines by:

- Category
- Brand/company
- Medicine/product
- Other useful catalog attributes supported by the imported data

IMPORTANT:

Do not create fake categories, fake brands, fake medicines, fake prices, fake stock, or sample data.

The SDF files are the source of truth.

IMPORTER REQUIREMENTS:

- Process large SDF files using bounded memory.
- Do not load the entire files and all parsed records into memory simultaneously.
- Use streaming/chunked processing where appropriate.
- Use database transactions/batches appropriately.
- Make the import idempotent.
- Running the same import twice must not create duplicate medicines, companies, categories, or relationships.
- Preserve stable source identifiers wherever available.
- Report inserted/updated/skipped/error counts.

MEDICINE IMAGES:

I will provide ONE specific medicine image.

Use that exact image as the default image for all imported medicines/products initially.

Do NOT generate random medicine images.
Do NOT scrape images from the internet.
Do NOT assign different fake images.

Store the image through the project's existing image/storage architecture.

Make the image field replaceable later so individual medicines can receive their own images from the admin system.

SEARCH:

After importing, connect the existing medicine search to the imported database records.

I need the actual imported medicines to appear on the deployed website.

Verify that searching for medicines works after import.

IMPORTANT DEPLOYMENT REQUIREMENT:

The production website must use the same production database/API containing the imported medicine catalog.

Do not import the data only into Replit Preview.

Do not create a development-only medicine database that Vercel cannot access.

Before deployment, clearly identify:

- Which database contains the imported medicines
- Which API reads those medicines
- Which Vercel frontend route/component displays them

VERIFICATION:

Before declaring this complete:

1. Parse the supplied SDF files.
2. Report total records discovered in each file.
3. Report medicines/products imported.
4. Report companies/brands imported.
5. Report categories imported.
6. Report drug groups imported.
7. Report stock records imported.
8. Verify medicine → category relationships.
9. Verify medicine → company/brand relationships.
10. Verify medicine search.
11. Verify category filtering.
12. Verify brand/company filtering where supported.
13. Run TypeScript checks.
14. Run production build.
15. Run database/API checks.
16. Verify the same import does not create duplicates.

DO NOT deploy yet.

DO NOT delete the old database.
DO NOT delete existing project functionality.
DO NOT create fake data.
DO NOT introduce recurring inventory synchronization.

First inspect the repository and report the existing architecture and exactly where the new medicine importer should live.

STOP after the inspection if anything is ambiguous. Do not make unrelated changes.